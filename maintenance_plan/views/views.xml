<odoo>
  <data>
    <!-- explicit list view definition -->
    <!--
    <record model="ir.ui.view" id="maintenance_plan.list">
      <field name="name">maintenance_plan list</field>
      <field name="model">maintenance_plan.maintenance_plan</field>
      <field name="arch" type="xml">
        <tree>
          <field name="name"/>
          <field name="value"/>
          <field name="value2"/>
        </tree>
      </field>
    </record>
    -->

    <!-- actions opening views on models -->
    <!--
    <record model="ir.actions.act_window" id="maintenance_plan.action_window">
      <field name="name">maintenance_plan window</field>
      <field name="res_model">maintenance_plan.maintenance_plan</field>
      <field name="view_mode">tree,form</field>
    </record>
    -->

    <!-- server action to the one above -->
    <!--
    <record model="ir.actions.server" id="maintenance_plan.action_server">
      <field name="name">maintenance_plan server</field>
      <field name="model_id" ref="model_maintenance_plan_maintenance_plan"/>
      <field name="state">code</field>
      <field name="code">
        action = {
          "type": "ir.actions.act_window",
          "view_mode": "tree,form",
          "res_model": self._name,
        }
      </field>
    </record>
    -->

    <!-- Top menu item -->
    <!--
    <menuitem name="maintenance_plan" id="maintenance_plan.menu_root"/>
    -->
    <!-- menu categories -->
    <!--
    <menuitem name="Menu 1" id="maintenance_plan.menu_1" parent="maintenance_plan.menu_root"/>
    <menuitem name="Menu 2" id="maintenance_plan.menu_2" parent="maintenance_plan.menu_root"/>
    -->
    <!-- actions -->
    <!--
    <menuitem name="List" id="maintenance_plan.menu_1_list" parent="maintenance_plan.menu_1"
              action="maintenance_plan.action_window"/>
    <menuitem name="Server to list" id="maintenance_plan" parent="maintenance_plan.menu_2"
              action="maintenance_plan.action_server"/>
    -->
  </data>
</odoo>